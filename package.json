{
  "name": "jshandwritting",
  "version": "1.0.0",
  "description": "Convert octave code to JS and see how it does (does not do well!)",
  "keywords": ["octave", "js", "handwritting"],
  "license": "MIT",
  "author": "Daniel Chapman",
  "scripts": {
    "start": "parcel index.html — open",
    "build": "parcel build index.html",
    "dev": "npm run build && npm run start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild" : "parcel build index.html"
  },
  "dependencies": {
    "mathjs": "^3.20.2",
    "ml-util": "^1.1.1",
    "parcel-bundler": "^1.12.4"
  },
  "browserslist": [
    "since 2017-06"
  ],
  "engines": {
    "node": "14.x"
  }
}
